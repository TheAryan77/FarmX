{
  "name": "@fasalx/blockchain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "build": "echo \"[blockchain] no contracts yet — FasalXEscrow.sol arrives in session 9\"",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.2",
    "hardhat": "^2.26.3",
    "typescript": "^5.9.2",
    "viem": "^2.37.5"
  }
}
