/**
 * Shared FasalX TypeScript types.
 *
 * CLAUDE.md: shared types live here and are imported by apps — never duplicated.
 * Populated per vertical slice. Empty barrel for the scaffold slice.
 */

export {};
