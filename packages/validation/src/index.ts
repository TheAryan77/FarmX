/**
 * Shared FasalX Zod schemas.
 *
 * CLAUDE.md: Zod schemas live here and are shared by the API and the clients.
 * Populated per vertical slice. Empty barrel for the scaffold slice.
 */

export {};
